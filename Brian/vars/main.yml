---
# --- Database Configuration ---
db_name: "wordpress_db"
db_user: "wp_admin"
db_password: "password"
db_root_password: "rootpassword"

# --- WordPress Configuration ---
wp_version: "5.0"
wp_download_url: "https://wordpress.org/wordpress-{{ wp_version }}.tar.gz"
wp_install_dir: "/var/www/html"

# --- Vulnerability Injection ---
# Mailpoet is an exploitable plugin 
vuln_plugin_name: "mailpoet"
vuln_plugin_url: "https://downloads.wordpress.org/plugin/wysija-newsletters.2.6.7.zip"

# --- System Settings ---
web_user: "www-data"
web_group: "www-data"